# Instructions for Gemini Assistant

Do NOT use `--no-verify` on git commits. All checks in the pre-commit hook must pass.
If a check fails (e.g., outdated dependencies, lint errors, or build failures), you must address the issue instead of bypassing it.
